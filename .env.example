BMC_HOST=10.0.99.215
BMC_USER=root
BMC_PASS=changeme
# Optional: password-protect the VNC/noVNC session itself. Leave blank
# for no VNC password (fine if this only listens on a trusted host).
VNC_PASSWORD=
